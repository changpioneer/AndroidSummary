给EditText设置android:imeOptions=""，键盘会变化GO、DONE等等。


```xml
 <EditText
        android:id="@+id/actionDone"
        android:hint="actionDone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:imeOptions="actionDone"
        />
```